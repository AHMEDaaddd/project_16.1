[tool.poetry]
name = "oop-inheritance"
version = "0.1.0"
description = "SkyPro HW 16.1 — Product Inheritance"
authors = ["Ахмаджу <you@example.com>"]
packages = [
    { include = "src", from = "." }
]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3"
pytest-cov = "^4.1"
flake8 = "^7.3.0"
black = "^25.1.0"
isort = "^6.0.1"
mypy = "^1.16.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
